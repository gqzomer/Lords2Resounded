[project]
name = "r2r-preprocess"
version = "0.1.0"
description = ""
authors = []
requires-python = ">=3.11"
dependencies = [
    "dpdfnet (>=0.6.0,<0.7.0)"
]

[project.scripts]
r2r-preprocess = "r2r_preprocess.cli:main"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"