[project]
name = "Lords2R"
version = "0.1.0"
description = ""
authors = []
requires-python = ">=3.11,<3.15"
dependencies = [
    "dpdfnet (>=0.6.0,<0.7.0)",
    "lavasr @ git+https://github.com/ysharma3501/LavaSR.git"
]

[tool.poetry]
packages = [{ include = "Lords2R" }]

[project.scripts]
upsample = "Lords2R.cli:main"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"